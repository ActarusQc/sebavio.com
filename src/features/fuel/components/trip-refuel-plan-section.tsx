"use client";

/**
 * Le plan détaillé est intégré dans TripFuelSettingsCard (accordéon).
 * Conservé pour compat / variant stacked du panneau estimation.
 */
export function TripRefuelPlanSection() {
  return null;
}

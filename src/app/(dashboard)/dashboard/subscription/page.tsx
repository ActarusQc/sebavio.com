import { ModulePlaceholder } from "@/components/layout";

export default function SubscriptionPage() {
  return (
    <ModulePlaceholder
      title="Abonnement"
      description="Forfait et facturation (feature subscriptions)."
    />
  );
}

import { ModulePlaceholder } from "@/components/layout";

export default function SettingsPage() {
  return (
    <ModulePlaceholder
      title="Paramètres"
      description="Profil et préférences (feature users)."
    />
  );
}

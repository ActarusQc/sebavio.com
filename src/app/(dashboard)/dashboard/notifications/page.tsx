import { ModulePlaceholder } from "@/components/layout";

export default function NotificationsPage() {
  return (
    <ModulePlaceholder
      title="Notifications"
      description="Centre de notifications (feature notifications)."
    />
  );
}

import { ModulePlaceholder } from "@/components/layout";

export default function CatalogPage() {
  return (
    <ModulePlaceholder
      title="Catalogue véhicules"
      description="Constructeurs et modèles (feature vehicle-catalog)."
    />
  );
}
